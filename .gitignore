config.json
.env
*.log
!testdata/psfixtures/**

# goreleaser snapshot/release output — never committed
/dist/

# session handoff snapshot — shared via hosted onboarding link, not tracked
ONBOARDING.md
CLAUDE.md

# local planning docs — not tracked
plans/
